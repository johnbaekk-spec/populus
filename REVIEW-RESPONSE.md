# Response to External Review — ARCHITECTURE v2.0 → v2.1

**Date:** 2026-07-16 · **Reviewed artifact:** ARCHITECTURE.md v2.0 (SHA-256 `59d4c618…`, 480 lines) · **Disposition:** all 15 findings + accuracy defects addressed in **v2.1**, which supersedes v2.0 in place. The directory is now a git repository (initialized this session; first commit pending owner) so future reviews have a diffable baseline.

**Summary: 14 of 15 findings accepted (4 with refinements), 1 accepted-with-correction where the review itself contained a stale fact (F7 value units — evidence below). The review was high quality; F2 (CORS), F5 (row_seq instability), F7 (13F amendment types), and F13 (sample-size math) would each have produced latent data corruption or a broken consumer had they survived to implementation.**

| # | Finding | Disposition | Where in v2.1 |
|---|---|---|---|
| **F1** | Spec not self-contained ("unchanged from v1.0" references a nonexistent doc) | **Accepted.** All normative content inlined: full M1 DDL, pipeline behavior, CLI contracts, tool specs, caching, publishing. v2.1 contains zero references to v1.0 as a normative source. Git repo initialized for future baselines. | Entire doc; §9 (M1) in particular |
| **F2** | Pattern F can't power the static dashboard (SEC has no CORS) | **Accepted — design changed.** Patterns are now declared **per consumer** (DR-10, §5.6 consumer-access matrix). MCP stays live-federated; the dashboard receives bounded build-time extracts. Newly verified enabler: SEC's nightly bulk `companyfacts.zip` (HTTP 206 probe, `Content-Length: 1,389,620,072`) lets the M3 dashboard extract be built from one bulk download instead of thousands of API calls. `/financials` static commitment is retained but bounded to a curated universe (§10.3). | DR-10, §5.6, §10.3, §12 |
| **F3** | Legal scope rule contradicts M1; missing license machinery | **Accepted.** Scope test rewritten to "**redistributable under recorded conditions**"; new conditions register (`licenses.json`, machine-readable, shipped in every build) with per-source `license_id`, permitted uses, required notices, verbatim attribution text; `license_id` on artifacts and records; `DATA-LICENSE.md` + `NOTICE` files specified; BLS's specific retrieval-date + disclaimer requirements captured; "public record ≠ public domain" distinction recorded (17 U.S.C. §105 covers government works, not third-party filings — our determinations now state the actual basis per source); **counsel review moved to a pre-public-launch gate (P2), not pre-monetization**; kadoa and FRED regularized as register entries under a rewritten G2 instead of ad-hoc exceptions. | §2.2, §15, G2, P2 gate |
| **F4** | No safe versioned artifact protocol | **Accepted.** New §5.5: immutable builds with `build_id`; a single per-build `manifest.json` (SHA-256 + byte size per artifact, module schema versions, source watermarks, client-compat range, `previous_build_id`); publish order assets→manifest→`latest.json` pointer last; client protocol download-to-temp → hash/size verify → `PRAGMA integrity_check` → atomic rename, prior cache retained on any failure; ≥90-day retention; rollback = repoint runbook; consumers never mix artifacts across builds. Rollback and schema-compat behavior are now P1/P2 gates with drills. | §5.5, §17 gates |
| **F5** | `row_seq` identity unstable; parse/lifecycle conflated; no kadoa lineage; OGE dead weight | **Accepted.** `txn_id` now = `filing_id : row_fingerprint` (SHA-256 over the raw field tuple, ordinal-disambiguated); reparse is an **atomic per-filing replace** (transactional delete+insert — no ghost rows, no ID drift); `lifecycle ∈ {active, superseded, retired, withdrawn}` is a separate column from `parse_status` (which is outcome-only); amendment behavior is **verify-first** (new OQ-13 with a conservative both-visible-flagged default until House/Senate semantics are established from real amended filings); kadoa lineage via `filings.source='kadoa'` + `primary_filing_id` crosswalk + retire-don't-delete tombstones; **OGE rows are not imported** (raw seed file archived only). | §9.4–9.6, OQ-13 |
| **F6** | GitHub treated as unlimited CDN | **Accepted with refinement.** Git now holds only manifests, small JSON, and registries; SQLite snapshots and raw-archive bundles move to **immutable Release assets** (DR-5 amended); measured thresholds (repo >1 GB or clone >2 min or any throttling signal → migration trigger, named successor Cloudflare R2 free tier ≤10 GB); growth tracked in `stats.json`. Refinement: at M1's kB/day delta the risk was years away — but the protocol is needed for F4 anyway, so the same mechanism solves both. | DR-5, §6, §13.4 |
| **F7** | M2 stale (datasets page live, ~95 MB/qtr) and amendment model wrong | **Accepted, with one correction to the review.** Datasets page re-verified HTTP 200 (my earlier 503 was transient — OQ-10 closed); the M2 contract now models `amendmentType` (**RESTATEMENT = supersede; NEW HOLDINGS = merge**), confidential-treatment omissions, `otherManager`/related-filer double-count avoidance, `sshPrnamtType`. **Correction:** "VALUE is reported in thousands" is true only for pre-2023 filings; post-modernization filings report whole dollars — verified arithmetically on the pulled 2026 Berkshire infotable (ALLY: value 498,992,850 ÷ 12,719,675 sh = $39.23/sh; thousands would imply $39k/sh). v2.1 specifies **era-dependent unit normalization** (`unit_basis` column) covering both regimes. | §10.2 |
| **F8** | Identifier model not historically safe | **Accepted.** §5.4 rebuilt as a temporal identity model: `entities` (CIK-anchored) and `securities` as separate identities; all mappings (ticker↔CIK, CUSIP↔security) carry `valid_from/valid_to`, provenance, confidence, review state; one-to-many supported; historical joins resolve **as-of the record's date**; silent CUSIP→current-ticker→CIK chaining prohibited (now guardrail G14); unresolved = name-only + flag. | §5.4, G14 |
| **F9** | Provenance insufficient for mutable APIs / derived data | **Accepted.** Provenance model extended: source record/accession IDs, exact request parameters, response content hash, effective date/vintage where the source has revision semantics, `parser_version` + `normalization_version`, and derived-aggregate lineage (input build + query identity). | §5.1 |
| **F10** | Pattern F aggregate-load and UA claims overstated | **Accepted.** "At any scale"/"never loads agencies" language removed. Federated client now specifies: default ≤2 req/s to SEC (5× under the 10 req/s ceiling), response caching, request coalescing, bulk-endpoints-first, and a UA policy where each installation identifies itself with a per-install ID and a **user-supplied contact** (`POPULUS_CONTACT`, warned-if-absent) rather than one shared project contact masquerading as a single operator. | §11.4, §12.2 |
| **F11** | Cloudflare Pages hard limits ignored | **Accepted.** Limits recorded (20k files, 25 MiB/file, 500 builds/mo, 20-min builds); every module contract now carries a **page budget**; global cap 15k files (75% of limit) tracked in `stats.json`; long-tail strategy defined: static pages for bounded cores (M1 all members+active tickers ≈ <5k files; M2 top filers; M3 curated universe), client-side rendering from published JSON on a generic route for everything else. | §7, §12.1 |
| **F12** | Watchdog not independent; publishing coordination undefined | **Accepted.** External heartbeat: a Mac-mini launchd job (fully outside GitHub) polls `latest.json`/`stats.json` age and fires Discord alerts — live-by-P1 gate. Added: Actions `concurrency` group serializing all publishes; push-conflict rebase-retry(3)-then-fail; secrets inventory corrected to **three** (Discord webhook, Pages deploy hook, fine-grained PAT scoped `populus-data:contents` for mini fallback publishing); recovery/backfill runbook. | §13.2–13.5 |
| **F13** | 50-row audit can't establish ≤2% error | **Accepted — math conceded.** (0/50 → ~5.8% one-sided 95% upper bound; the gate as written was innumerate.) Replaced with an acceptance-sampling plan: stratified by chamber × year-band × asset class × filing route, **n=150 with zero critical-field errors** (95% UB ≈ 1.97% < 2% target); critical fields enumerated (member identity, ticker, side, amount bucket, both dates); cosmetic errors tracked separately, ≤5%. | §9.6 |
| **F14** | No supply-chain/publication security | **Accepted.** New §14: least-privilege workflow `permissions:` blocks; publish jobs isolated from PR-triggered jobs (no secrets in PR context, `pull_request_target` banned); third-party Actions pinned to full SHAs; branch protection + CODEOWNERS on parsers/aliases/licenses/workflows; `uv.lock` hashes + dependency/license audit in CI; release attestations + manifest checksums (consumers verify — F4); deploy-hook rotation policy; user-side cache written `0600`. Security checklist is a P1 gate. | §14 |
| **F15** | Unmeasurable exit gates | **Accepted.** Every gate quantified (examples: MCP cold-start ≤60 s clean machine; snapshot-tool p95 ≤2 s, federated p95 ≤4 s / ≤1 s warm-cache; M3 "graceful" = 3 named sparse-tagging companies return flagged partials with zero exceptions; macro series equal agency values on 3 dated reference points). Added gates: schema-compat fail-safe test (previous released client vs new manifest), rollback drill, disaster-recovery reconstruction drill (raw → DB ≤2 h, counts match manifest), source-completeness reconciliation, amendment-correctness fixtures, security checklist, license-conformance (100% artifacts carry `license_id`), page-budget check, external-monitor-live. | §17 |
| Accuracy | BEA "verified" overstated; OQ-10/App-B.6 stale; Appendix C tense; OGE wording; superlatives; MCP-registry `server.json` + PyPI ownership marker; PyPI ≠ reservation | **All fixed.** BEA marked signup-page-only; OQ-10 closed with today's re-check; Appendix C says "must be verified in P1"; OGE "not imported"; superlatives converted to dated observations ("as of 2026-07-16 searches"); registry requirements (versioned `server.json`, PyPI README ownership marker) added to P2 scope+gate; **P0 now contains a naming action** — publish a `populus-mcp 0.0.1` placeholder to PyPI upon approval (outward-facing: owner executes or explicitly delegates). | throughout |

**Not changed, deliberately:** one-module-at-a-time sequencing, parse-or-flag, dual-date honesty, Compass separation, the R/F distinction (now per-consumer), and the $0 posture — the review endorsed these, and the fixes above were made *within* them.
