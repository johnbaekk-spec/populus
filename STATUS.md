# Populus — Build Status (autonomous build session, 2026-07-22 → 07-24)

**Bottom line:** M1 (congressional trading) is **COMPLETE — all 6 build-runs merged to `main`**, **936 tests green**, and the whole system is **integration-verified end-to-end on real government data**: ingest (House 312 PTRs + Senate 53 + kadoa + members) → stats → build → publish → verify (906 artifacts, pointer v2), and the **`populus-mcp` server serving that published snapshot** over stdio (8 tools, honest envelope, real records). Every run passed external Codex review. **M2 (institutional 13F) is also COMPLETE — all 4 runs merged.** M3 (company financials), M4 (macro), the P3 dashboard, and the OQ-1 domain remain (see Pending).

> **Update 08-01c (M1-D — orphan context hijack fixed; the last 3 eras are OWNER DECISIONS, not defects):** The M1-C follow-up predicted "split amount cells never rejoin". That was the symptom; the cause sat a level up. A row that runs off the bottom of a page has its **cap-gains glyph REPRINTED** on the continued page; the row's `capgains_cell` is already filled, so the duplicate completes nothing and opens an orphan — and `_open_orphan()` reassigned `open_candidate`, so the row's *real* continuation line (asset tail + the second half of a split amount) attached to the orphan. One printed row became three records with the amount stranded as `'$15,001 -'`. Fix: while a **structural** candidate's block is still open it remains the continuation context; the orphan is still appended and still flagged (R25/F4 visibility untouched) but no longer steals context. Scoped — once a sub-line closes the block the prior behaviour stands, pinned by `test_a_closed_block_still_hands_context_to_the_orphan`. Reparse of 8,298 filings, `failed 0`: **orphans 632 → 558, `amount_unparsed` 711 → 561**, 2019 96.0→**96.8%**, 2018 94.7→95.1%, 2020 95.5→95.9%. **Published build `20260802.1`, pointer v6, verify ok. 1,809 tests green.** PARSER_VERSION 1.1.0→1.2.0. **Eras passing stays 11/14 — and the residue is deliberately NOT closed by me, because both parts are owner decisions rather than defects:** (1) **exact dollar amounts** (99/105/226 in 2018/19/20) — `'$94.91'`, `'$505.24'` — read *perfectly* by the parser but not Appendix C range buckets, so they score as parse defects; reclassifying them would make all three eras pass **without improving a single parsed value**, which is gate-weakening and the owner's call; (2) **wrapped tails of NON-comment sub-lines** (144/67/67, equal counts again) continuing `SUBHOLDING OF:`/`LOCATION:` — M1-C excluded these on purpose since those values are stored nowhere, so folding a tail in would **delete** text; giving them a home needs a schema addition (`filing_status`, `subholding_of`, `location`), a feature not a fix. Record appended to [M1-C-subline-wrap-decision.md](docs/build/M1-C-subline-wrap-decision.md). **Senate re-probed and still down** — homepage 200, search POST 503 (tested on a scratch DB, corpus untouched).
>
> **Update 08-01b (M1-C — THE NINE FAILING ERAS WERE ONE PARSER BUG; 11/14 NOW PASS):** The 2016–2023 "template drift" diagnosis was **wrong**, and measuring it rather than assuming it is what closed it. Four defect flags stood at *exactly* 1,761 across the miss band — one mechanism, not five. Reading the source PDFs found it: a long `DESCRIPTION:`/`COMMENTS:` sub-line **wraps like any other printed line**, and its tail arrived after the row block had closed, so it fell through to `_open_orphan()` and was emitted as a flagged **transaction row carrying no side, no date, no amount**. The parser was manufacturing 1,761 disclosures the documents never made. Instrumented over 400 random miss-band PDFs: **673 continuations, 641 comment tails, none resembling a transaction row** (e.g. *"the S & P 500. I learned of the purchase of stock on Oct. 21, and immediately on the next busin…"*). Fix: a fragment is appended to the comment it continues when a **comment** sub-line is open, the wrap geometry allows it, and **no cell matches its own column signature** (`_has_typed_cell`, reusing the R25 recognizer table so "row-shaped" cannot drift between this decision and the structural one). **This REVERSES the reviewed R7/F4 "post-comment text is never absorbed" control from `2195982`** — not silently: decision record at [M1-C-subline-wrap-decision.md](docs/build/M1-C-subline-wrap-decision.md), and the control's actual concern (a genuine malformed row staying visible) is preserved by the typed-cell condition and pinned by two new tests, the sharpest being an **amount**-bearing fragment (R24 forbids an amount opening a row structurally, so nothing but `_has_typed_cell` keeps it out of the comment). Narrowed twice while working: **comment sub-lines only** (folding a `FILING STATUS:` tail would *delete* text — the silent loss F4 guards — costing ~5% of the repair, 32 of 673, to avoid dropping a printed character), and **signature-based not position-based** (wrapped prose spans the full page width, so position alone fails). Reparse of all 8,298 House filings from the raw archive, `failed 0`: **orphan rows 2,510 → 632**, `v_default_transactions` **60,357 → 58,479** (the drop IS the repair — phantom rows returned to their comments), **eras passing the 0.97 gate 5/14 → 11/14**. 2023 **85.9% → 99.3%**; 2025 93.3% → 99.6%; 2016/2017/2021/2022 also cross. **1,807 tests green.** PR [#6](https://github.com/johnbaekk-spec/populus/pull/6). Two operational findings recorded: the corpus draws on **two** raw archives (historical fetch + the published build's own cache), so reparse needed a unified root and a preflight that resolves every path before writing; and **Senate remains blocked by an eFD outage confirmed with a control** — all 14 windows 2013–2026 fail identically (handshake 200→302, search POST 503), *including the 2026 window that succeeded hours earlier*, so it is eFD-side, not our request pattern. **Remaining 2018/2019/2020 misses are a DIFFERENT mechanism, measured and scoped as M1-D, not left vague:** split amount cells that never rejoin, proven by exact pair counts (`'$15,001 -'` ×32 / `'$50,000'` ×32; `'$100,001 -'` ×6 / `'$250,000'` ×6) on R24's completion oracle — a different code path with its own over-joining risk.
>
> **Update 08-01 (PHASE B COMPLETE — FULL 2013–2026 CORPUS PUBLISHED, pointer v4):** House Phase B finished **12/12 years, zero failures** (~55 min at the measured 0.414 s/req). The corpus grew **3,911 → 60,357 default-view transactions across 8,601 active filings**. Per-era 0.97 parse gate, all denominators fully known: **5 pass** (2014 98.4%, 2015 97.8%, 2024 98.3%, 2026 97.5%, Senate-2026 100%), **9 miss** (2016–2023 template-drift band 85.9–96.9%, 2025 93.3%) — defective rows loaded and flagged, never dropped. **Owner decision: publish era-scoped now**; M1-C parser extension queued for the 2016–2023 template era. Unification: `phase-a.db` had no inst tables, so the 13 historical years were re-ingested into a backup copy of the canonical DB **at zero transport** (filesystem checkpoint sidecars settled every document from disk — 20 minutes, no re-contact with the Clerk), parity exact (60,357 / 602,496). **Published: build `20260801.1`, pointer v4, verify ok (4,184 artifacts)** — congress 2013–2026 era-scoped + inst module unchanged (0.9853 certifiable). The build measured a **§9.10 budget breach: 4,183 files vs the ≤4,000 M1 cap** — the 13-year ticker tail is 3,856 tickers (vs ~2,500 assumed; members 321 vs ~700). **Owner decision: cap raised to ≤8,500 (full coverage, ~2.4× Cloudflare headroom)** — applied to ARCHITECTURE §9.10, `accept_m1_b.py`, its tests, and the dashboard `DEFAULT_ENTITY_PAGE_BUDGET` (3,800 → 8,300). Record: [RUN-M1-B-phase-b.md](docs/build/RUN-M1-B-phase-b.md). Open: Senate on eFD recovery (one-command); M1-C; 2013 paper-era OCR backlog.
>
> **Update 07-31 (FOUR RUNS MERGED + FIRST INST PUBLISH + PHASE B RUNNING):** A single pipeline session merged **M2-6** (bulk 13F: EDGAR discovery, survivor-sum ranking, checkpoint-before-bytes journaled driver — 8-round orchestrated QA carousel on the harness docs-commit skew documented, substantive bridge review 4 blockers → APPROVED), **M2-7** (cover-noise tolerance, owner decision 2026-07-31: exact-integer max($1k, 0.1%) rounding tolerance, max(T,S) denominators, view-level conflict exclusion named on all six surfaces, ensure_views staleness replacement — spec-first, 3 bridge rounds → APPROVED), and **M1-B** (congressional backfill Phase A: shared checkpoint primitives, verified-settled + provenance-boundary spec after a third-round carousel, two-census 0.97 era gate, Senate window seam — 4 review rounds incl. one owner-authorized past cap → APPROVED zero findings). **THE INSTITUTIONAL MODULE IS PUBLISHED AND SERVING**: build `20260731.1`, pointer v3 — 1,000/1,000 top filers, 602,496 holdings, coverage **0.9853 certifiable**, 4 rounding filings tolerated (max delta $12), 3 cover conflicts excluded and NAMED, 983 filers answering through the real MCP snapshot path. **Phase A measured**: House 2015 **PASSES at 97.8%** (46.4% paper counted, not parsed; the unmeasurable rule fired live on a 403 and resolved honestly); Senate window blocked by an eFD 503 source outage (one-command recovery). Owner decisions recorded: tolerance+flag; House Phase B authorized (RUNNING — 2013–2025 at the measured 0.414 s/request); Senate on eFD recovery. Ops incidents survived and documented: two crashed orchestrate runs recovered without losing paid work, a model-provenance false positive (CLI-internal haiku tokens) discarding a $34 dev phase (tree recovered; tool fix chipped), five API stream stalls incl. one that left a live mutant on disk — caught by mandatory disk verification before any gate ran. Records: docs/build/RUN-M2-6-*, RUN-M2-7-*, RUN-M1-B-* incl. the phase-a decision record and both mechanism specs.
>
> **Update 07-30 (M2-5 MERGED — the inst coverage gate now PASSES):** **RUN M2-5 — identity coverage via the SEC Official 13(f) List — is COMPLETE and merged to `main` (`57a88b5`).** Quarterly Official Lists (text + PDF, mandatory cross-format identity on the dual-format quarter, 25,333 rows) seed quarter-exact CUSIP validity intervals; `resolve_cusip` is precedence-aware and fail-closed; measured value-coverage on the real Berkshire corpus is **0.9996 corpus-wide (0.9988/1.0000/1.0000 per period)** and **`inst` is admitted to the published manifest on BOTH rollout orders**; uncovered quarters still fail closed by name. `sec-13f-list` register entry ships with an explicit **CUSIP-redistribution counsel flag** (CGS/ABA notice on the PDF cover) for the P2 counsel gate. **1578 tests green; 7/7 reintroduced mutants killed.** Process honestly stated: orchestrated plan (5 Codex rounds) → Opus dev → orchestrate crashed mid-fix (stream abort; an orphaned fix child kept editing and its work was verified, not trusted) → recovery via the codex-review bridge: substantive round 1 found **10 blockers** (weak gates made mandatory, silent acceptances made loud, an order-dependence bug, a lifecycle test bypassing real ingest, and an undeclared per-row-SQL debt hidden behind a false `executemany` claim — recorded as TD-M2-5-3 resolved), one batched remediation, round 2 **APPROVED with zero findings**. Two orchestrate-tool bugs found (review-output-v1 phase allowlist rejects `code-review`; `awk` PATH in the evidence validator) — the bridge fail-safe recorded CHANGES_REQUESTED on a schema-valid APPROVED document. Records: [plan](docs/build/RUN-M2-5-plan.md), [dev notes](docs/build/RUN-M2-5-devnotes.md), [resolution map](docs/build/RUN-M2-5-review-resolution.md), [approved verdict](docs/build/RUN-M2-5-review-approved.txt). **Next: RUN M2-6 (bulk 13F corpus → first real inst publish), then RUN M1-B (congressional 2013–2025 backfill)** — briefs in docs/build/.
>
> **⚠ M2 KNOWN ISSUES — four findings shipped open: [`docs/build/M2-KNOWN-ISSUES.md`](docs/build/M2-KNOWN-ISSUES.md).** A **second, independent** review of the same M2-5 code — scoped to the implementation only, with harness/provenance checking explicitly out of scope — did **not** reach the round-2 "APPROVED, zero findings" verdict above: it returned **2 blockers and 6 majors**, and separately re-verified the round-1 fixes as 10 VERIFIED-FIXED / 5 PARTIALLY-FIXED / 1 NOT-FIXED. Both reviews are recorded; the disagreement is itself a finding, and the likely cause is that an approving round can spend its budget on bundle provenance instead of the code. Carried open: **KI-1** an invalid status cell collapses to the same canonical value as a blank one, so the R5 cross-format gate can declare two *different* files identical; **KI-2** a row damaged before the fixed status slice hides an A/D conflict, seeding a disputed identity (that rule fires 6–76×/quarter on real data); **KI-3** the SEC `Total Count` trailer check is skipped whenever extraction returns `None`; **KI-4** an inflated filing still *publishes* `coverage = 1.2` — the gate fails closed correctly but the printed ratio exceeds 100%, a **G5 honesty defect and the one to fix first**, since unlike KI-1..3 it needs no malformed input. KI-1/KI-2 were accepted at merge on a **measurement**: 167,083 rows across all seven cached SEC files contain **zero** malformed rows, so neither has a trigger in any list published to date — but their failure mode is silent, so re-run that count on each newly cached quarter and stop if it is non-zero. Mechanism-level remediation is specified (unreviewed) in [`docs/build/RUN-M2-5-parse-substrate.md`](docs/build/RUN-M2-5-parse-substrate.md).
>
> **Update 07-25 (M2-4 MERGED — MODULE 2 COMPLETE):** **RUN M2-4 — inst MCP tools + federated per-filer detail + M2 envelope — is COMPLETE and merged to `main` (`ba7209b`).** Five `inst_*` tools over the published aggregate plus a live-SEC federated detail path, and a **rebuilt `SnapshotClient` serving lifecycle**. **1475 tests green; dep_guard clean.** Records: [plan](docs/build/RUN-M2-4-plan.md), [dev notes](docs/build/RUN-M2-4-devnotes.md), [**serving-lifecycle specification**](docs/build/RUN-M2-4-withdrawal-lifecycle.md). **All four M2 runs are merged — Module 2 (institutional 13F) is complete.**
>
> **The lesson of this run: specify first.** QA rounds 5–8 each found blockers in ONE mechanism — a tombstone scheme inferring "is this module withheld?" from four independently-corruptible files. Round 8's four blockers were all inside the same function, rewritten four times. Instead of a fifth patch the lifecycle was **specified** (§1 invariant: *a module is served ONLY while the client can positively prove, from durable state, that the current trust-anchored pointer publishes it*), **design-reviewed through six revisions to APPROVED**, then implemented — deleting the tombstone, sidecar and `current` marker in favour of one anchor-bound record. **Four subsequent independent reviewers attacked that subsystem and could not break it.** The same treatment was later applied to amendment composition (a 17-case truth table + pipeline-agreement suite) after it recurred three times. Every defect found *after* a specification existed was in code that never got one.
>
> **⚠ BEHAVIOUR CHANGE — TD-7, affects ALL modules including congress.** A corrupt *or deleted* trust anchor beside any local cache state now **REFUSES** rather than re-bootstrapping. The old fail-open path demonstrably served coverage-gate-**withheld** data stamped `inst_from_published_manifest=True` with the ≥95% guarantee, reachable via a stale mirror or a replayed still-attested pointer. Recovery: confirm the data repo's `latest.json` is current, then remove the whole module cache directory. A genuinely fresh cache still bootstraps normally.
>
> **Verification, stated honestly.** Eight orchestrated QA rounds (35 blockers) + **seven independent review rounds** (24 blockers, 30 nits); every finding fixed and **mutation-verified**. The review count did not converge — rounds ran 4, 3, 2, 2, 5, 5, 6 blockers — and rounds C–G all landed in the **presentation layer** (how data is *described*), never in the specified lifecycle. Also caught and recorded rather than smoothed over: **eleven non-load-bearing tests** (seven found by reviewers, including two whose own mutation checks passed because the test was circular or its fixture created no divergence) and **six false factual claims in the dev notes** — three about test coverage, three about the changed-file list, one of those *despite a standing instruction in that very list to reconcile against `git status`*. Round G's closing structural guidance for whoever extends this layer is recorded verbatim in the dev notes.

> **Update 07-25 (M2-3 MERGED — first fully-earned APPROVAL of M2):** **RUN M2-3 — cross-filer aggregates + multi-module publish — is COMPLETE and merged/pushed (`62f4794`), APPROVED by external review** ("the implementation completely satisfies the approved plan… I would merge this as-is"). Ships `inst_agg.db` (filer registry, QoQ deltas, per-issuer top-holders, per-filer concentration) with a three-pass identity matcher, multi-module publish (module-admitting manifest, module-aware `db_path()`, inst logical digest, two-build reproducibility, generalized rollback + extra-module asset preflight on BOTH the publish and reconcile paths), and **the ≥95% coverage gate exactly as the owner decided — fail-closed, inst withheld on the FTD-only corpus, reason surfaced at both build and publish.** **1298 tests green.** Records: [plan](docs/build/RUN-M2-3-plan.md), [dev notes](docs/build/RUN-M2-3-devnotes.md), [R10a acceptance](docs/build/RUN-M2-3-R10a-acceptance.txt).
>
> **What the 8 QA rounds / 48 findings actually caught** (each behavioural fix mutation-verified): a **data-destroying** `inst-agg --out` alias that unlinked and replaced the SOURCE database; silent coverage inflation from a missing cover total; SH and PRN positions merged into meaningless share deltas; QoQ fabricating continuity across a skipped quarter; two distinct securities collapsed by a too-permissive CUSIP bridge; partial remote mutation on a resumed publish; and a documented recovery procedure that **reused an immutable build identity for different bytes** (now fixed with a durable per-date allocation high-water mark). Several were in code written to fix earlier findings, and two were vacuous tests caught by mutation testing.

> **Update 07-24 (M2-2 MERGED):** **RUN M2-2 — 13F ingest/parse/normalize + golden fixtures — is COMPLETE and merged/pushed (`18aeeaa`).** Turns EDGAR 13F filings into queryable holdings on the M2-1 substrate: index-discovered info-table filenames (never hardcoded), era-keyed `unit_basis` (2023-01-03 cutover), typed amendments (RESTATEMENT supersede / NEW HOLDINGS merge incl. confidential-treatment), affiliated-filer de-dup, `v_default_inst_filings`/`v_default_holdings`, as-of CUSIP identity (G14), full §5.1 provenance + atomic load, and never-inflated coverage inputs. **1247 tests green.** Plan APPROVED by Codex on plan-review round 5; the run then took **5 QA rounds / 23 findings, all fixed**, each behavioural fix **mutation-verified**. Records: [plan](docs/build/RUN-M2-2-plan.md), [dev notes + V-A transcript](docs/build/RUN-M2-2-devnotes.md).
>
> **V-A acceptance on REAL data:** 4 Berkshire filings, 314 holdings, 0 failures, amendment linked, and **Σ`value_usd` exactly equal to each cover total (delta 0 on every filing)**.
>
> ### ⚠ OWNER DECISION AT M2-3 ENTRY — the ≥95% coverage gate is unreachable from FTD alone
> Measured, not assumed. With only a June-2026 FTD archive, value-coverage was **0.00%** (correct fail-closed: FTD intervals were 2026-06-15→07-01 while report periods are 2025-03/2025-12/2026-03 — no identity time-travel, G14). Adding a **period-covering** archive proved the machinery: **0% → 16.33%**, and for 2026-Q1 alone **42/90 rows (46.7%) / 49.46% by value**. Root cause of the ceiling: for that period **29/29 of its distinct CUSIPs are in the registry but only 15 are valid as-of the period date** — FTD records a CUSIP only on days it actually failed to deliver, so as-of intervals are inherently sparse (~50% by value). **M2-3 executes the gate and will fail closed forever on FTD data.** Options: (a) admit an identifier-history source with real validity intervals via §15; (b) add an explicitly-labelled, confidence-carrying inference layer (G5 tension); (c) revisit the gate's basis/threshold. M2-2's own obligation — never-inflated inputs — is met.

> **Update 07-24 (M2 started — M2-1 MERGED):** Module 2 (institutional 13F) kicked off. Phase-entry §7 contract + fresh live SEC source verification recorded ([docs/build/M2-CONTRACT.md](docs/build/M2-CONTRACT.md)); M2 decomposed into 4 runs. **RUN M2-1 — identity registries + conservative SEC federated client + conditions register — is COMPLETE and merged to `main` (`481f30b`)**: §5.4 temporal identity substrate (entities/securities/security_identifiers/entity_tickers + supersessions ledger, fail-closed as-of resolution G14), the declared-authority security-identity model (`securities.yaml` + interval-cutting migration), FTD + company_tickers bootstrap, the §11.4 SEC client (SEC-accepted UA — the parenthesized M1 form 403s, corrected here; ≤2 req/s floor, single-flight, ETag cache, backoff, 403 breaker, Retry-After both forms), and §15 `sec-edgar`/`sec-ftd` register entries. **1157 tests green; dep_guard clean; SEC-UA live smoke 200.** Doer = **Opus 4.8** (`CLAUDE_MODEL=opus`, no Fable); reviewer = Codex sol xhigh.
>
> **M2-1 process note & lesson.** orchestrate's auto-fix loop branches from `main`, so it can't fix code on a feature branch; M2-1 ran plan→(cap)→pre-approved dev, then a **QA-only + hand-fix loop** (7 Codex rounds). It hardened real bugs (fail-open on disputed/continuity, retained-owner supersession on split+rename, title-conflict ordering, deterministic non-NULL raw, CIK/date canonicalization, endpoint-TTL host case, UTC as-of default, replay-zero accounting) but did **not converge to a zero-findings verdict** — Codex-sol-xhigh keeps surfacing peripheral/latent edges on dense identity code (count oscillated 2↔5). **Owner accepted M2-1 (2026-07-24)** at the bar *"gates green + no v1-reachable data-correctness blocker."* One latent, **v1-unreachable** determinism edge (multi-value-security `review_state` last-write-wins; `securities.yaml` ships empty so no multi-value securities exist yet) is documented as **TD-M2-1-9**, to fix in M2-2 when the authority is populated. Full record: [docs/build/RUN-M2-1-devnotes.md](docs/build/RUN-M2-1-devnotes.md). *Implication for M2-2..4: budget for the same asymptotic QA dynamic, or adopt the pragmatic acceptance bar up front.*
>
> **Update 07-24:** Run 6 (MCP server) is done and merged (PR #6). It was hand-built directly (not via orchestrate) because a **monthly Fable spend limit** blocked orchestrate's `claude -p` subprocesses mid-session; the quality bar was held with the full test suite + Codex review (Codex is a separate subscription). Codex ran five review rounds on it and caught three genuine correctness/honesty bugs — a `since_iso` timezone-comparison error, an over-claiming `data_note`, and mishandled open-ended dollar ranges (which dropped the largest trades) — all fixed with regression tests, then APPROVED. **If the orchestrated process bar matters for parity, Run 6 can be re-run through orchestrate.sh once the spend limit resets; the code is already merged and green.**

## What is built, merged, and verified

All on `~/projects/populus` `main` (private GitHub: `johnbaekk-spec/populus`). Each run went through the full orchestrate.sh loop — Fable plan → Codex (gpt-5.6-sol xhigh) plan review to approval → Fable dev → deterministic gates → Opus QA synthesis → Codex QA review to approval → squash-merge — and I independently re-ran the suite + real-corpus acceptance before each merge.

| Run | PR | Scope | Real-data result |
|---|---|---|---|
| 1 substrate | #1 | schema (§9.4 DDL), RFC-8785 canonical identity, atomic per-filing load, CLI skeleton, G1 dep-guard | 148 tests |
| 2 house | #2 | House ZIP index + PTR PDF ingest/classify/parse/normalize, golden corpus, reconciliation | **97.5%** e-file parse coverage on the real 312-PTR 2026 corpus (P1 gate ≥97%) |
| 3 senate | #3 | eFD session handshake, DataTables watermark query, 9-col HTML parser, paper→needs_ocr, politeness floors + circuit breaker | **100%** e-file parse on the real 53-filing corpus |
| 4 members/backfill | #4 | congress-legislators join (temporal aliases), kadoa import (OGE excluded) + crosswalk + sealed-draw audit sampler, amendment views, stats, license register | **100%** member-join coverage (P1 gate ≥98%) |
| 5 publish | #5 | §5.5 build/manifest/signed-pointer state machine/logical digest, §12.1 dist-digest+inventory, crash-consistent snapshot client, journal publish + fresh-runner recovery, external monitor, workflow files, licensing artifacts | 918 tests; see note below |
| 6 mcp | #6 | populus-mcp: FastMCP stdio, 6 congress tools + member_flows + populus_health, honest envelope (both dates, doc_url, ranges incl. open-ended, license_notices, lag note); snapshot-client or --db | 18-case suite incl. 10-q real-corpus golden; live stdio smoke-tested on the published snapshot; Codex-APPROVED |

**End-to-end integration test (real data, this session), on merged `main`:**
`db init → ingest house(312) + senate(53) + backfill + members → stats → build → publish → verify` all succeed — build `20260724.1`, 906 artifacts, logical digest computed, pointer v1, verify recomputed all 906 artifacts OK. The pipeline is real and working.

**Test suite:** `uv run pytest -q` → **918 passed** on `main`. `scripts/dep_guard.py` clean (no paid/vendor deps — G1).

### Note on Run 5 (publish protocol)
The most security-sensitive subsystem; it took **nine external Codex review rounds** (QA-only mode), every finding fixed and verified load-bearing, hardened by class: uncaught decode/sqlite, auth-token scoping (token only to api.github.com, stripped on cross-origin redirect), deep JSON shape validation, path containment via single chokepoints (`_safe_path`/`_safe_under`/`resolve_within` + strict build_id/module grammar), cache/pointer trust-binding, manifest completeness, publish/rollback ordering, owned-base symlink refusal. An independent Opus QA synthesis returned **PASS (no functional defects)**. Two items are **owner-accepted, documented** boundaries (in `docs/runbooks/disaster-recovery.md` + the RUN-5 dev notes), reaffirmed under review:
1. The pre-draft crash boundary is benign safe-refuse+rebuild, not same-build_id recovery (committing the ~35 MB DB-inlining journal to git would regress DR-5 git-bloat).
2. The configured `data_repo`/`state_dir` roots themselves are trusted operator inputs; a symlinked configured-root is out of the §14 threat model (precondition = publisher-filesystem compromise). Populus-owned base subdirs/files *are* symlink-refused.
The merge was made on this convergent evidence (918 green + nine all-fixed Codex rounds + Opus PASS + documented out-of-scope perimeter) after tooling flakiness (a transient Codex failure and a synthesis heading-schema error) denied a clean tenth machine-verdict; the substance was unambiguous.

## Constraint hit during the build (historical)

**Monthly Fable 5 spend limit reached mid-session** (during Run 6 planning). The orchestrate loop spawns headless `claude -p` (Fable) subprocesses for plan/dev/fix/synthesis; once the cap was hit these failed immediately with *"You've hit your monthly spend limit."* — a billing cap, not a technical issue (I did not bypass it). Consequence: **Runs 1–5 were built via the full orchestrate loop; Run 6 was hand-built directly in-session** and gated with the full test suite + Codex review (Codex is a separate subscription, unaffected). Run 6 is merged and green; re-running it through orchestrate for process parity is optional (see Pending #1).

## Pending

**All open work now lives in [`BACKLOG.md`](BACKLOG.md)** — the four M2 findings
carried open at merge (start with **B1/KI-4**, the published-coverage-above-100%
honesty defect), the process items, the owner P0 actions (PyPI name, domain), and
the M3/M4 roadmap. The former contents of this section are consolidated there.

Real source corpus is cached under `~/projects/Populus/data-cache/` (gitignored): House 2013/2015/2020/2026 indexes + 312 2026 PTR PDFs, 53 Senate pages, kadoa `trades.json`, legislators YAML. Golden fixtures are committed under `tests/fixtures/`. To run the whole thing yourself: `uv run pytest -q`, then the ingest→build→publish→verify sequence in this file, then `uv run populus-mcp --data-repo ../populus-data` (or `--db <ingested.db>`).

*Tooling note:* two orchestrate.sh bugs found and fixed during the build (committed in `~/projects/orchestrate-tool`): a `RESOLUTION NOTES` heading case/markdown-tolerance gate, and a reviewer-prompt that mis-flagged stripped plan revisions. A `WORKFLOW_MAX_ARTIFACT_BYTES=8388608` override is needed for runs with large golden-fixture diffs.
