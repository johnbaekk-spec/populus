# RUN M2-11 — Release-Hygiene Finalization Owner Decision

**Date:** 2026-08-11
**Owner authorization:** “Authorize one exceptional owner-reviewed release-hygiene repair and QA round 8, limited to removing the 13 Markdown trailing-space errors, with no product changes or T0 rerun.”

The owner authorizes one bounded repository repair and one eighth logical round
of the separate M2-11 QA/docs finalization cycle. The substantive repair is
limited to deleting exactly two trailing ASCII space bytes from each of 13
already-approved Markdown lines across eight governance artifacts. The evidence
transport may add only the new decision/plan, exact round-8 authority and
predecessor handling in the existing M2-11 evidence runner, focused removal-fails
coverage, and factual Dev Notes/QA-report propagation required to re-establish
independent approval after those repository bytes change.

The sealed round-7 QA approval, sealed docs-attempt-2 approval, exact 70-path
tree, and failed post-stage release check remain immutable historical evidence.
Round 8 must consume the exact sealed docs-attempt-2 approval through a
release-hygiene predecessor contract, bind one exact release-gate resolution,
use a new create-once `qa-v9-finalization-round-8` output, rerun all 15 standing
gates, and receive fresh independent QA and docs reviews before release resumes.

This decision does not authorize product, schema, payload, database, serving,
aggregate, build-resource, workflow, runbook, acceptance, security, or
deployment-policy changes; another T0/full-corpus run; snapshot mutation;
validation relaxation; evidence deletion or overwrite; self-approval; a ninth
QA round; a fourth docs attempt; or release before both new reviews approve.

The controlling plan is
`docs/build/RUN-M2-11-QA-finalization-release-hygiene-plan.md`; all prior plans,
decisions, bundles, reviews, logs, T0-v11, and the snapshot remain immutable
historical evidence.
